feat: Add score to Excel exports, fix FAT formatting, and improve category filtering

Excel Export Enhancements:
- Added Score column to Excel exports (both Animal Entry and Mating Calculator)
- Excel exports now sorted by score descending (highest to lowest) by default
- Fixed score values not appearing in Excel export by syncing scores to scrapedData
- Score column formatted with 2 decimal places in Excel

FAT EPD Formatting:
- Updated FAT EPD values to display with 3 decimal places (instead of 2) everywhere
- Applied to: main comparison table, Excel exports, mating calculator displays, and detail modals
- All other traits remain at 2 decimal places

Category Filtering Fix:
- Fixed mating ranking category filtering to work with multi-category system
- Updated filter logic to check categories array instead of single category field
- Maintains backward compatibility with old single-category format
- Resolves "No sires found in cache" error when using category filtering

Documentation:
- Added SCORING_FORMULA.md with complete mathematical formula for scoring algorithm
- Includes all constants, step-by-step calculations, and worked examples
- Suitable for sharing with partners and stakeholders

Technical Changes:
- Enhanced main/main.js: 
  - Added score sorting and FAT formatting to Excel export
  - Fixed category filtering in rank-all-matings to use categories array
- Enhanced renderer/renderer.js:
  - Sync scores to scrapedData after calculation for Excel export
  - Updated all FAT EPD formatting to 3 decimal places
  - Added score to mating export data structure
- Enhanced main/cache-util.js: Already supports categories array (no changes needed)
